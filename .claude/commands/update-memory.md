This session is wrapping up. Review what was accomplished and update the memory system per the CLAUDE.md instructions.

Steps:
1. Run `git diff HEAD~3` and `git log --oneline -10` to see recent changes and commits from this session.
2. Read the current state of the memory/data files:
   - `.context/data/sessions.md` — append a session summary
   - `.context/data/decisions.md` — append any decisions made with reasoning
   - `.context/data/lessons.md` — append any hard-won knowledge or mistakes caught
3. Check if any `.context/modules/` files need targeted updates based on what changed (new patterns, conventions, architecture shifts).
4. For each proposed update: state which file changes and what the change is, then wait for approval before writing.
5. Update `todo.md` and `plans.md` if tasks were completed or new ones emerged.

Additional context from the user (if any): $ARGUMENTS
